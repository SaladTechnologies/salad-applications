import { connect } from '../../connect'
import { MobileTitlebar } from './components/MobileTitlebar'

const mapStoreToProps = () => ({})

export const MobileTitlebarContainer = connect(mapStoreToProps, MobileTitlebar)
