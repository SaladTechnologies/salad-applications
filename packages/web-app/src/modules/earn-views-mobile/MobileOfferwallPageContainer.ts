import { connect } from '../../connect'
import { MobileOfferwallPage } from './pages'

const mapStoreToProps = (): any => ({})

export const MobileOfferwallPageContainer = connect(mapStoreToProps, MobileOfferwallPage)
