export interface SaladPaymentResponseDetails {
  /**
   * The unique token for the transaction. This is what is
   *  used to verify the transactions on your server
   * */
  transactionToken: string
}
