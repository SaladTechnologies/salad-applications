Desktop App

const createMainWindow = () => {
    ...

    bridge.on('get-bridge-name', () => {
        console.log('Do stuff on the electron side here')
        
        // If you need to return data back to the UI
        bridge.send('set-bridge-name', blob)
    })

    ...
}

Web App

const getBridgeName = 'get-bridge-name'
const setBridgeName = 'set-bridge-name'

constructor(...) {
    if (this.isNative) {
        // Response from the get request 
        this.on(setBridgeName, (service: Services[]) => {
            this.setBridgeName(service)
        })
    }
}

@action // Makes the bridged callback to the Desktop App
getBridgeName = (services: string) => {
    this.send(getBridgeName, services)
}

@action
setBridgeName = (services: Services[]) => {
    // Do stuff with the returned value/object from set
}
